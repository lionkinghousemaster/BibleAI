"""Image Engine（預留）。

尚未遷入：圖片生成邏輯（`ImageProvider`/`DummyProvider`/`ComfyUIProvider`/
`generate_image_from_prompt`/`generate_scene_image`）仍在專案根目錄的
`generate_image.py`。未來遷入時會把它移到這裡，並在此 re-export，維持
與 `engine.prompt` 相同的對外介面風格。
"""
